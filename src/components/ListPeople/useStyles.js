import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  title: {
    fontSize: 24,
  },
  data: {
    fontSize: 18,
  },
  wrapper: {
    marginTop: 20,
  },
});
